<% total = 0
	if posts 
		posts.each do |post| 
			if post.category1 == "4"
				total += post.price
			else
				total -= post.price
			end
    end
	end %>
<td class="left-justify"><%= total %>円</td>